{% macro getProbationPractitionerLink(allocated, name, licenceId) %}
    {% if allocated %}
        <a href="/licence/approve/id/{{ licenceId }}/probation-practitioner" class="govuk-link">{{ name }}</a>
    {% else %}
        {{ name }}
    {% endif %}
{% endmacro %}
