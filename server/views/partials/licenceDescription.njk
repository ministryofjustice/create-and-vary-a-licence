{% macro licenceDescription(typeCode, isLao, laoEnabled) %}
    {% if typeCode === 'AP' %}
        Standard determinate
    {% elseif typeCode === 'AP_PSS' %}
        Standard determinate<br/>
        Post sentence supervision
    {% elseif typeCode === 'PSS' %}
        Post sentence supervision
    {% elseif laoEnabled and isLao == true %}
        Restricted
    {% else %}
        {{ typeCode }}
    {% endif %}
{% endmacro %}
